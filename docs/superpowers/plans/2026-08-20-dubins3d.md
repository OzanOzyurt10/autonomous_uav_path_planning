# Dubins Airplane (3B) Uygulama Planı

> **Bu projede kod Ozan tarafından yazılır.** Claude testleri yazar, çalıştırır, geri bildirim verir ve commit eder; fonksiyon gövdelerini Ozan yazar. Adımlarda kimin yazacağı OZAN / CLAUDE olarak belirtilmiştir.
>
> **Not:** OZAN adımlarında bilerek hazır gövde verilmiyor — imza, ne yapılacağının adım adım tarifi ve tuzaklar veriliyor. Bu kasıtlı; hazır blok kopyalanınca yazma pratiği kayboluyor.

**Amaç:** 2B Dubins yolunu irtifa boyutuyla genişletmek — sabit kanatlı bir uçağın tırmanma açısı sınırına uyan, `(x, y, z, yaw)` pozları arasında yol üreten model.

**Mimari:** Yeni `src/dubins3d.py`, mevcut `dubins.py`'yi **yatay bileşen** olarak kullanır. Geometri yeniden yazılmaz; 3B sınıf yatay yolun üstüne irtifa katmanı koyar. Yol kısa kalırsa başlangıçta tam turlar (helis) eklenerek uzatılır.

**Teknoloji:** Python 3.11+, standart kütüphane (`math`, `dataclasses`), pytest, matplotlib (yalnız demo).

**Spec:** `docs/superpowers/specs/2026-08-20-dubins3d-design.md`

## Genel Kısıtlar

- Kod ve docstring **ASCII** — Türkçe karakter kullanılmaz.
- Docstring'ler kısa: 3-5 satır, yalnızca koddan anlaşılmayan gerekçe.
- `raise ValueError(...)`, `return ValueError(...)` değil.
- Mantıksal koşullarda `and` / `or`, `&` / `|` değil.
- Modül seviyesindeki tanımlar arasında iki boş satır; `if` gövdesi alt satırda.
- `dubins.py` **değiştirilmez**; yalnızca açık arayüzü kullanılır (`DubinsPath`, `shortest_path`, `path_length`).
- Poz düzeni: `(x, y, z, yaw)`, ENU, yaw radyan, z yukarı pozitif.
- `gamma_max` radyan; geçerli aralık `(0, pi/2)`.

## Dosya Yapısı

| Dosya | Sorumluluk |
|---|---|
| `src/dubins3d.py` | 3B yol modeli (yeni) |
| `tests/test_dubins3d.py` | Testler (yeni) |
| `notebooks/dubins3d_demo.py` | 3B görselleştirme (yeni) |

## Test Sabitleri

Test dosyasının başına, bir kez:

```python
"""src/dubins3d.py icin testler."""

import math
import random

import pytest

from src.dubins import DubinsPath, path_length, shortest_path
from src.dubins3d import DubinsPath3D      # her gorevde yeni isim eklenir

RHO = 5.0
GAMMA_MAX = math.radians(15.0)
TWO_PI_RHO = 2 * math.pi * RHO


def _flat(pose3):
    """3B pozdan 2B poz: (x, y, z, yaw) -> (x, y, yaw)."""
    return (pose3[0], pose3[1], pose3[3])


def _manual(start3, goal2, helix_turns, gamma):
    """airplane_path olmadan elle DubinsPath3D kurar (erken gorevler icin)."""
    return DubinsPath3D(start3, shortest_path(_flat(start3), goal2, RHO),
                        helix_turns, gamma)


def assert_pose3_close(actual, expected, tol=1e-6):
    for i, ad in enumerate(("x", "y", "z")):
        assert math.isclose(actual[i], expected[i], abs_tol=tol), \
            f"{ad}: {actual} vs {expected}"
    dyaw = (actual[3] - expected[3]) % (2 * math.pi)
    dyaw = min(dyaw, 2 * math.pi - dyaw)
    assert dyaw < tol, f"yaw: {actual} vs {expected}"
```

---

### Görev 1: Modül iskeleti ve `DubinsPath3D`

**Dosyalar:**
- Oluştur: `src/dubins3d.py`
- Test: `tests/test_dubins3d.py`

**Arayüz:**
- Tüketir: `src.dubins.DubinsPath`
- Üretir: `Pose3`, `DubinsPath3D(start, horizontal, helix_turns, gamma)`

- [ ] **Adım 1: Başarısız testi yaz — CLAUDE**

```python
class TestDubinsPath3D:
    def test_fields_are_positional_in_order(self):
        h = shortest_path((0.0, 0.0, 0.0), (30.0, 0.0, 0.0), RHO)
        p = DubinsPath3D((0.0, 0.0, 100.0, 0.0), h, 2, 0.2122)
        assert p.start == (0.0, 0.0, 100.0, 0.0)
        assert p.horizontal is h
        assert p.helix_turns == 2
        assert math.isclose(p.gamma, 0.2122)

    def test_is_frozen(self):
        h = shortest_path((0.0, 0.0, 0.0), (30.0, 0.0, 0.0), RHO)
        p = DubinsPath3D((0.0, 0.0, 0.0, 0.0), h, 0, 0.0)
        with pytest.raises(Exception):
            p.gamma = 1.0

    def test_horizontal_is_a_two_d_path(self):
        h = shortest_path((0.0, 0.0, 0.0), (30.0, 0.0, 0.0), RHO)
        p = DubinsPath3D((0.0, 0.0, 0.0, 0.0), h, 0, 0.0)
        assert isinstance(p.horizontal, DubinsPath)
        assert len(p.horizontal.start) == 3      # 2B poz, z yok
```

- [ ] **Adım 2: Testi çalıştır, başarısız olduğunu gör — CLAUDE**

- [ ] **Adım 3: Modülü kur — OZAN**

Yazılacaklar, sırayla:

1. **Modül docstring'i.** Ne olduğu (Dubins airplane), `dubins.py`'yi yatay
   bileşen olarak kullandığı, poz düzeninin `(x, y, z, yaw)` olduğu.
2. **Import'lar:** `math`, `dataclasses`'tan `dataclass`, ve
   `src.dubins`'ten `DubinsPath`, `path_length`, `shortest_path`.
3. **Tip takma adı:** `Pose3 = tuple[float, float, float, float]`
4. **`DubinsPath3D`** — `@dataclass(frozen=True)`, dört alan bu sırayla:

```
start        -> Pose3
horizontal   -> DubinsPath
helix_turns  -> int
gamma        -> float
```

**Class docstring'de tek şey:** `horizontal` mevcut 2B yol, z bilmiyor; 3B
sınıf onun üstüne irtifa katmanı koyuyor.

- [ ] **Adım 4: Testi çalıştır, geçtiğini gör — CLAUDE**

- [ ] **Adım 5: Commit — CLAUDE**

```bash
git add src/dubins3d.py tests/test_dubins3d.py
git commit -m "Dubins3D: modul iskeleti ve DubinsPath3D"
```

---

### Görev 2: `_helix`

**Dosyalar:**
- Değiştir: `src/dubins3d.py`
- Test: `tests/test_dubins3d.py`

**Arayüz:**
- Tüketir: `src.dubins.DubinsPath`
- Üretir: `_helix(start2: tuple[float, float, float], direction: str, length: float, rho: float) -> DubinsPath`

- [ ] **Adım 1: Başarısız testi yaz — CLAUDE**

```python
class TestHelix:
    def test_returns_to_start_pose(self):
        start = (3.0, 7.0, math.radians(40))
        for k in (1, 2, 3):
            h = _helix(start, "L", k * TWO_PI_RHO, RHO)
            assert_pose3_close(h.end_pose() + (0.0,), start + (0.0,))

    def test_length_matches_requested(self):
        h = _helix((0.0, 0.0, 0.0), "L", 2 * TWO_PI_RHO, RHO)
        assert math.isclose(h.length, 2 * TWO_PI_RHO)

    def test_right_direction_also_returns(self):
        start = (1.0, 2.0, 0.5)
        h = _helix(start, "R", TWO_PI_RHO, RHO)
        assert_pose3_close(h.end_pose() + (0.0,), start + (0.0,))

    def test_turns_the_requested_way(self):
        """L saat yonunun tersine, R saat yonunde."""
        quarter = TWO_PI_RHO / 4
        left = _helix((0.0, 0.0, 0.0), "L", quarter, RHO)
        right = _helix((0.0, 0.0, 0.0), "R", quarter, RHO)
        assert left.interpolate(quarter)[1] > 0      # sola donunce y artar
        assert right.interpolate(quarter)[1] < 0     # saga donunce y azalir

    def test_zero_length_is_a_point(self):
        h = _helix((4.0, 5.0, 1.0), "L", 0.0, RHO)
        assert math.isclose(h.length, 0.0)
        assert h.end_pose() == (4.0, 5.0, 1.0)

    def test_word_is_valid(self):
        assert _helix((0.0, 0.0, 0.0), "L", 1.0, RHO).word == "LSL"
        assert _helix((0.0, 0.0, 0.0), "R", 1.0, RHO).word == "RSR"
```

`assert_pose3_close(h.end_pose() + (0.0,), start + (0.0,))` numarası: yardımcı
dört elemanlı poz bekliyor, 2B pozlara sahte bir z ekleniyor.

- [ ] **Adım 2: Testi çalıştır, başarısız olduğunu gör — CLAUDE**

- [ ] **Adım 3: `_helix`'i yaz — OZAN**

Tam imza:
```python
def _helix(start2: tuple[float, float, float], direction: str,
           length: float, rho: float) -> DubinsPath:
```

**Ne yapacak:** verilen yönde `length` metre dönen bir `DubinsPath` üretip
döndürecek. Tek satır — bir `DubinsPath` kurup `return` etmek.

`DubinsPath`'in dört argümanı: `start`, `word`, `lengths`, `rho`.

- `start` → gelen `start2`
- `word` → `direction` + `"S"` + `direction`, yani `"LSL"` veya `"RSR"`.
  Python'da string'ler `+` ile birleşir.
- `lengths` → üç elemanlı **demet**: ilk eleman `length`, diğer ikisi `0.0`.
  Demet parantezle yazılır: `(a, b, c)`
- `rho` → gelen `rho`

**Neden bu işe yarıyor:** `rho` yarıçaplı tam bir dönüş `2*pi*rho`
uzunluğunda ve başlangıç pozuna birebir döner. İkinci ve üçüncü parçalar sıfır
uzunlukta olduğu için yalnızca ilk parça yürünür.

Docstring: helisin bitiş pozunu değiştirmeden yatay uzunluk eklediği.

- [ ] **Adım 4: Testi çalıştır, geçtiğini gör — CLAUDE**

- [ ] **Adım 5: Commit — CLAUDE**

```bash
git add src/dubins3d.py tests/test_dubins3d.py
git commit -m "Dubins3D: helis yardimcisi (_helix)"
```

---

### Görev 3: `horizontal_length` ve `length`

**Dosyalar:**
- Değiştir: `src/dubins3d.py`
- Test: `tests/test_dubins3d.py`

**Arayüz:**
- Üretir: `DubinsPath3D.horizontal_length` (property), `DubinsPath3D.length` (property)

- [ ] **Adım 1: Başarısız testi yaz — CLAUDE**

```python
class TestLengths:
    def test_no_helix_horizontal_equals_two_d(self):
        p = _manual((0.0, 0.0, 0.0, 0.0), (100.0, 0.0, 0.0), 0, 0.099669)
        assert math.isclose(p.horizontal_length, 100.0)

    def test_helix_adds_full_turns(self):
        p = _manual((0.0, 0.0, 0.0, 0.0), (30.0, 0.0, 0.0), 2, 0.212200)
        assert math.isclose(p.horizontal_length, 30.0 + 2 * TWO_PI_RHO)

    def test_length_is_horizontal_over_cos_gamma(self):
        p = _manual((0.0, 0.0, 0.0, 0.0), (30.0, 0.0, 0.0), 2, 0.212200)
        assert math.isclose(p.length, p.horizontal_length / math.cos(p.gamma))

    def test_length_matches_pythagoras(self):
        """length == sqrt(H^2 + dz^2), dz = H*tan(gamma)."""
        p = _manual((0.0, 0.0, 0.0, 0.0), (30.0, 0.0, 0.0), 2, 0.212200)
        dz = p.horizontal_length * math.tan(p.gamma)
        assert math.isclose(p.length, math.hypot(p.horizontal_length, dz))

    def test_zero_gamma_length_equals_horizontal(self):
        p = _manual((0.0, 0.0, 0.0, 0.0), (40.0, 0.0, 0.0), 0, 0.0)
        assert math.isclose(p.length, 40.0)
        assert math.isclose(p.horizontal_length, 40.0)

    def test_descent_length_equals_climb_length(self):
        up = _manual((0.0, 0.0, 0.0, 0.0), (30.0, 0.0, 0.0), 2, 0.212200)
        down = _manual((0.0, 0.0, 0.0, 0.0), (30.0, 0.0, 0.0), 2, -0.212200)
        assert math.isclose(up.length, down.length)

    def test_known_values(self):
        p = _manual((0.0, 0.0, 0.0, 0.0), (30.0, 0.0, 0.0), 2, 0.212200)
        assert math.isclose(p.horizontal_length, 92.831853, abs_tol=1e-5)
        assert math.isclose(p.length, 94.961850, abs_tol=1e-5)
```

- [ ] **Adım 2: Testi çalıştır, başarısız olduğunu gör — CLAUDE**

- [ ] **Adım 3: İki özelliği yaz — OZAN**

İkisi de `DubinsPath3D` sınıfının **içinde**, alanların altında, `@property`
dekoratörüyle. `dubins.py`'deki `DubinsPath.length` aynı kalıpta yazılmıştı,
oraya bakabilirsin.

**`horizontal_length`:** yatay yolun uzunluğu artı helislerin uzunluğu. Helis
sayısı `self.helix_turns`, her biri `2 * math.pi * self.horizontal.rho`
uzunluğunda. `rho`'yu ayrı alan olarak tutmuyoruz — yatay yolun içinde zaten
var.

**`length`:** 3B uzunluk, `horizontal_length / math.cos(self.gamma)`.

Neden bu formül: yatayda `H` metre giderken `gamma` açısıyla tırmanılıyor,
gerçekte katedilen yol hipotenüs. `sqrt(H^2 + dz^2)` ile aynı sonucu verir
(`dz = H*tan(gamma)`), ama tek işlemle.

**Tuzak:** `@property` ile yazılan şey fonksiyon gibi çağrılmaz —
`p.length()` değil `p.length`. `dubins.py`'de de öyle.

- [ ] **Adım 4: Testi çalıştır, geçtiğini gör — CLAUDE**

- [ ] **Adım 5: Commit — CLAUDE**

```bash
git add src/dubins3d.py tests/test_dubins3d.py
git commit -m "Dubins3D: yatay ve 3B uzunluk ozellikleri"
```

---

### Görev 4: `interpolate` ve `end_pose`

**Dosyalar:**
- Değiştir: `src/dubins3d.py`
- Test: `tests/test_dubins3d.py`

**Arayüz:**
- Tüketir: `_helix`, `DubinsPath.interpolate`
- Üretir: `DubinsPath3D.interpolate(s) -> Pose3`, `DubinsPath3D.end_pose() -> Pose3`

- [ ] **Adım 1: Başarısız testi yaz — CLAUDE**

```python
class TestInterpolate:
    def _climb(self):
        """30 m yatay yol, 2 helis, ~12.16 derece tirmanis."""
        return _manual((0.0, 0.0, 100.0, 0.0), (30.0, 0.0, 0.0), 2, 0.212200)

    def test_start_is_returned_at_zero(self):
        p = self._climb()
        assert_pose3_close(p.interpolate(0.0), p.start)

    def test_end_pose_matches_interpolate_at_length(self):
        p = self._climb()
        assert_pose3_close(p.interpolate(p.length), p.end_pose())

    def test_end_pose_xy_yaw_come_from_horizontal(self):
        p = self._climb()
        hx, hy, hyaw = p.horizontal.end_pose()
        end = p.end_pose()
        assert math.isclose(end[0], hx, abs_tol=1e-9)
        assert math.isclose(end[1], hy, abs_tol=1e-9)
        assert math.isclose(end[3], hyaw, abs_tol=1e-9)

    def test_end_altitude_follows_gamma(self):
        p = self._climb()
        expected = p.start[2] + p.horizontal_length * math.tan(p.gamma)
        assert math.isclose(p.end_pose()[2], expected, abs_tol=1e-9)

    def test_altitude_is_monotone_while_climbing(self):
        p = self._climb()
        zs = [p.interpolate(i / 40 * p.length)[2] for i in range(41)]
        assert zs == sorted(zs)

    def test_altitude_is_monotone_while_descending(self):
        p = _manual((0.0, 0.0, 100.0, 0.0), (30.0, 0.0, 0.0), 2, -0.212200)
        zs = [p.interpolate(i / 40 * p.length)[2] for i in range(41)]
        assert zs == sorted(zs, reverse=True)

    def test_helix_region_stays_near_start_xy(self):
        """Helis bolgesinde ucak baslangicin cevresinde donuyor."""
        p = self._climb()
        h_end = p.helix_turns * TWO_PI_RHO           # helis biterken yatay mesafe
        s = (h_end / 2) / math.cos(p.gamma)          # helisin ortasi
        x, y, _, _ = p.interpolate(s)
        assert math.hypot(x - p.start[0], y - p.start[1]) <= 2 * RHO + 1e-9

    def test_after_helix_follows_horizontal(self):
        p = self._climb()
        h_after = p.helix_turns * TWO_PI_RHO + 10.0
        x, y, _, yaw = p.interpolate(h_after / math.cos(p.gamma))
        hx, hy, hyaw = p.horizontal.interpolate(10.0)
        assert math.isclose(x, hx, abs_tol=1e-9)
        assert math.isclose(y, hy, abs_tol=1e-9)
        assert math.isclose(yaw, hyaw, abs_tol=1e-9)

    def test_clamps_out_of_range(self):
        p = self._climb()
        assert_pose3_close(p.interpolate(-5.0), p.start)
        assert_pose3_close(p.interpolate(p.length + 5.0), p.end_pose())

    def test_no_helix_path_ignores_helix_branch(self):
        p = _manual((0.0, 0.0, 50.0, 0.0), (100.0, 0.0, 0.0), 0, 0.099669)
        x, y, z, yaw = p.interpolate(p.length / 2)
        hx, hy, hyaw = p.horizontal.interpolate(50.0)
        assert math.isclose(x, hx, abs_tol=1e-9)
        assert math.isclose(y, hy, abs_tol=1e-9)
```

- [ ] **Adım 2: Testi çalıştır, başarısız olduğunu gör — CLAUDE**

- [ ] **Adım 3: `interpolate`'i yaz — OZAN**

Sınıfın içinde, `length` özelliğinin altında:

```python
    def interpolate(self, s: float) -> Pose3:
```

Adımlar:

1. **`s`'yi kırp.** `dubins.py`'deki `interpolate` ile aynı sözleşme: negatif
   değerler 0'a, `self.length`'ten büyük değerler `self.length`'e. `min` ve
   `max` ile tek satırda yapılıyor, `dubins.py`'ye bakabilirsin.
2. **Yatay mesafeyi hesapla:** `h = s * math.cos(self.gamma)`. Sebebi: 3B'de
   `s` metre giderken yatayda daha az yol alınır, aradaki oran `cos(gamma)`.
3. **İrtifayı hesapla:** başlangıç z'si artı `h * math.tan(self.gamma)`.
4. **Helis uzunluğunu hesapla:** `self.helix_turns * 2 * math.pi *
   self.horizontal.rho`.
5. **Yatay konumu bul, iki durum:**
   - `h` helis uzunluğundan küçükse: `_helix`'i çağırıp o yolun
     `interpolate(h)`'sini al. Helisin başlangıcı yatay yolun başlangıcı,
     yönü `self.horizontal.word[0]`.
   - Değilse: `self.horizontal.interpolate(h - helis_uzunlugu)`.
6. **Dördü birleştirip döndür:** `(x, y, z, yaw)` demeti.

**Üç tuzak:**

`self.horizontal.start` üç elemanlı `(x, y, yaw)` — `self.start`'ın z'si onda
yok. Helisi kurarken `self.horizontal.start`'ı kullan.

`word[0]` string'in ilk karakteri, yani `"L"` veya `"R"`.

Adım 5'in iki dalı da `(x, y, yaw)` döndürüyor; z'yi 3. adımdan alıyorsun.

- [ ] **Adım 4: `end_pose`'u yaz — OZAN**

```python
    def end_pose(self) -> Pose3:
```

Tek satır: `interpolate`'i yolun toplam uzunluğuyla çağırıp döndür.

- [ ] **Adım 5: Testi çalıştır, geçtiğini gör — CLAUDE**

- [ ] **Adım 6: Commit — CLAUDE**

```bash
git add src/dubins3d.py tests/test_dubins3d.py
git commit -m "Dubins3D: interpolate ve end_pose"
```

---

### Görev 5: `sample`

**Dosyalar:**
- Değiştir: `src/dubins3d.py`
- Test: `tests/test_dubins3d.py`

**Arayüz:**
- Tüketir: `DubinsPath3D.interpolate`, `DubinsPath3D.length`
- Üretir: `DubinsPath3D.sample(step) -> list[Pose3]`

- [ ] **Adım 1: Başarısız testi yaz — CLAUDE**

```python
class TestSample3D:
    def _climb(self):
        return _manual((0.0, 0.0, 100.0, 0.0), (30.0, 0.0, 0.0), 2, 0.212200)

    def test_first_and_last(self):
        p = self._climb()
        pts = p.sample(1.0)
        assert_pose3_close(pts[0], p.start)
        assert_pose3_close(pts[-1], p.end_pose())

    def test_spacing_never_exceeds_step(self):
        p = self._climb()
        pts = p.sample(2.0)
        for a, b in zip(pts, pts[1:]):
            d = math.sqrt((b[0] - a[0]) ** 2 + (b[1] - a[1]) ** 2
                          + (b[2] - a[2]) ** 2)
            assert d <= 2.0 + 1e-9

    def test_all_points_are_four_element(self):
        for pose in self._climb().sample(5.0):
            assert len(pose) == 4

    def test_zero_length_gives_single_point(self):
        p = _manual((1.0, 2.0, 3.0, 0.0), (1.0, 2.0, 0.0), 0, 0.0)
        assert p.sample(1.0) == [p.start]

    @pytest.mark.parametrize("bad", [0.0, -1.0])
    def test_bad_step_raises(self, bad):
        with pytest.raises(ValueError):
            self._climb().sample(bad)

    def test_smaller_step_gives_more_points(self):
        p = self._climb()
        assert len(p.sample(1.0)) > len(p.sample(5.0))
```

- [ ] **Adım 2: Testi çalıştır, başarısız olduğunu gör — CLAUDE**

- [ ] **Adım 3: `sample`'ı yaz — OZAN**

```python
    def sample(self, step: float) -> list[Pose3]:
```

`dubins.py`'deki `DubinsPath.sample` ile **birebir aynı mantık**; oraya bakıp
aynısını yazabilirsin. Sırayla:

1. `step <= 0` ise `raise ValueError(...)` — mesaja gelen değeri koy
2. Toplam uzunluk sıfırsa tek elemanlı liste döndür: `[self.start]`
3. Kaç parçaya bölüneceğini bul: `math.ceil(toplam / step)`
4. `0`'dan `n`'e kadar (n dahil) her `i` için `interpolate(i / n * toplam)`
   çağırıp listeye koy

Son adım liste kavramasıyla tek satır; `dubins.py`'de öyle yazılmış.

**`n + 1` nokta üretiliyor:** `n` parça varsa uçları da sayınca `n + 1` nokta
olur. `range(n)` yazarsan son nokta eksik kalır ve `test_first_and_last`
kırılır.

- [ ] **Adım 4: Testi çalıştır, geçtiğini gör — CLAUDE**

- [ ] **Adım 5: Commit — CLAUDE**

```bash
git add src/dubins3d.py tests/test_dubins3d.py
git commit -m "Dubins3D: sample"
```

---

### Görev 6: `airplane_path`

Modülün kalbi: durum ayrımı ve doğrulama.

**Dosyalar:**
- Değiştir: `src/dubins3d.py`
- Test: `tests/test_dubins3d.py`

**Arayüz:**
- Tüketir: `src.dubins.shortest_path`, `DubinsPath3D`
- Üretir: `airplane_path(start: Pose3, goal: Pose3, rho: float, gamma_max: float) -> DubinsPath3D`

- [ ] **Adım 1: Başarısız testi yaz — CLAUDE**

```python
class TestAirplanePathValidation:
    @pytest.mark.parametrize("bad", [0.0, -1.0])
    def test_bad_rho_raises(self, bad):
        with pytest.raises(ValueError):
            airplane_path((0.0, 0.0, 0.0, 0.0), (10.0, 0.0, 5.0, 0.0),
                          bad, GAMMA_MAX)

    @pytest.mark.parametrize("bad", [0.0, -0.1, math.pi / 2, 2.0])
    def test_bad_gamma_max_raises(self, bad):
        with pytest.raises(ValueError):
            airplane_path((0.0, 0.0, 0.0, 0.0), (10.0, 0.0, 5.0, 0.0),
                          RHO, bad)


class TestAirplanePathCases:
    def test_reachable_altitude_needs_no_helix(self):
        p = airplane_path((0.0, 0.0, 0.0, 0.0), (100.0, 0.0, 10.0, 0.0),
                          RHO, GAMMA_MAX)
        assert p.helix_turns == 0
        assert math.isclose(p.horizontal_length, 100.0, abs_tol=1e-6)
        assert math.isclose(p.gamma, 0.099669, abs_tol=1e-5)
        assert math.isclose(p.length, 100.498756, abs_tol=1e-5)

    def test_unreachable_altitude_adds_helix(self):
        p = airplane_path((0.0, 0.0, 0.0, 0.0), (30.0, 0.0, 20.0, 0.0),
                          RHO, GAMMA_MAX)
        assert p.helix_turns == 2
        assert math.isclose(p.horizontal_length, 92.831853, abs_tol=1e-5)
        assert math.isclose(p.gamma, 0.212200, abs_tol=1e-5)
        assert math.isclose(p.length, 94.961850, abs_tol=1e-5)

    def test_descent_mirrors_climb(self):
        p = airplane_path((0.0, 0.0, 20.0, 0.0), (30.0, 0.0, 0.0, 0.0),
                          RHO, GAMMA_MAX)
        assert p.helix_turns == 2
        assert math.isclose(p.gamma, -0.212200, abs_tol=1e-5)

    def test_level_flight_reduces_to_two_d(self):
        p = airplane_path((0.0, 0.0, 50.0, 0.0), (40.0, 0.0, 50.0, 0.0),
                          RHO, GAMMA_MAX)
        assert p.helix_turns == 0
        assert p.gamma == 0.0
        assert math.isclose(p.length, path_length((0.0, 0.0, 0.0),
                                                  (40.0, 0.0, 0.0), RHO))
        assert all(math.isclose(q[2], 50.0) for q in p.sample(1.0))

    def test_horizontal_is_the_two_d_shortest_path(self):
        start = (2.0, 3.0, 10.0, 0.4)
        goal = (40.0, 25.0, 30.0, 1.2)
        p = airplane_path(start, goal, RHO, GAMMA_MAX)
        expected = shortest_path((2.0, 3.0, 0.4), (40.0, 25.0, 1.2), RHO)
        assert p.horizontal.word == expected.word
        assert math.isclose(p.horizontal.length, expected.length)


class TestAirplanePathGeneral:
    def _pairs(self, seed=5, n=40):
        rng = random.Random(seed)
        for _ in range(n):
            yield ((rng.uniform(-50, 50), rng.uniform(-50, 50),
                    rng.uniform(0, 100), rng.uniform(0, 2 * math.pi)),
                   (rng.uniform(-50, 50), rng.uniform(-50, 50),
                    rng.uniform(0, 100), rng.uniform(0, 2 * math.pi)))

    @pytest.mark.parametrize("rho", [2.0, 5.0, 12.0])
    @pytest.mark.parametrize("gamma_deg", [5.0, 15.0, 30.0])
    def test_end_pose_reaches_goal(self, rho, gamma_deg):
        """En guclu test: yolun sonu hedefe variyor mu, dort bilesende."""
        gamma_max = math.radians(gamma_deg)
        for start, goal in self._pairs():
            p = airplane_path(start, goal, rho, gamma_max)
            assert_pose3_close(p.end_pose(), goal)

    @pytest.mark.parametrize("gamma_deg", [5.0, 15.0, 30.0])
    def test_gamma_never_exceeds_limit(self, gamma_deg):
        gamma_max = math.radians(gamma_deg)
        for start, goal in self._pairs():
            p = airplane_path(start, goal, RHO, gamma_max)
            assert abs(p.gamma) <= gamma_max + 1e-12

    def test_helix_overshoot_is_less_than_one_turn(self):
        """Nicemleme en fazla bir tur fazla atmali."""
        for start, goal in self._pairs():
            p = airplane_path(start, goal, RHO, GAMMA_MAX)
            if p.helix_turns == 0:
                continue
            dz = goal[2] - start[2]
            required = abs(dz) / math.tan(GAMMA_MAX)
            assert p.horizontal_length - required < TWO_PI_RHO + 1e-9
            assert p.horizontal_length >= required - 1e-9

    def test_altitude_is_monotone(self):
        for start, goal in self._pairs(seed=11, n=15):
            p = airplane_path(start, goal, RHO, GAMMA_MAX)
            zs = [q[2] for q in p.sample(2.0)]
            assert zs == sorted(zs) or zs == sorted(zs, reverse=True)
```

- [ ] **Adım 2: Testi çalıştır, başarısız olduğunu gör — CLAUDE**

- [ ] **Adım 3: Doğrulamayı yaz — OZAN**

Modül seviyesinde, sınıfın dışında:

```python
def airplane_path(start: Pose3, goal: Pose3, rho: float,
                  gamma_max: float) -> DubinsPath3D:
```

Gövdenin başına iki kontrol, `rrt_star.py`'deki kalıpla aynı — **`raise`,
`return` değil**, mesaja gelen değeri koy:

```
rho <= 0                                -> ValueError
gamma_max <= 0 veya gamma_max >= pi/2   -> ValueError
```

İkinci koşulda iki karşılaştırmayı **`or`** ile bağla, `|` ile değil.

`pi/2` neden dışlanıyor: `tan(pi/2)` tanımsız, fiziksel karşılığı dik tırmanış.

- [ ] **Adım 4: Durum mantığını yaz — OZAN**

Doğrulamadan sonra, sırayla:

1. **Yatay yolu kur.** `shortest_path` iki 2B poz istiyor; `start` ve `goal`
   dört elemanlı, ikisinden de `(x, y, yaw)` çıkarman gerekiyor —
   `(start[0], start[1], start[3])` gibi.
2. **İrtifa farkı:** `dz = goal[2] - start[2]`
3. **Gereken en az yatay yol:** `abs(dz) / math.tan(gamma_max)`
4. **Durum ayrımı:** yatay yol uzunluğu gerekenden büyük veya eşitse
   `helix_turns = 0`; değilse

   ```
   helix_turns = ceil((gereken - yatay_uzunluk) / (2*pi*rho))
   ```

   `math.ceil` yukarı yuvarlıyor. Sonuç `int` olmalı — `math.ceil` zaten `int`
   döndürüyor.
5. **Toplam yatay uzunluk:** yatay yol + `helix_turns * 2 * pi * rho`
6. **Açı:** `math.atan2(dz, toplam_yatay)`
7. **`DubinsPath3D(...)` kurup döndür** — dört argüman sırayla `start`,
   yatay yol, `helix_turns`, `gamma`.

**Üç tuzak:**

`math.atan2(y, x)` iki argüman alıyor ve işaretleri doğru yorumluyor;
`math.atan(dz / H)` da çalışır ama `H = 0` olursa patlar.

`dz = 0` iken gereken yol 0 çıkar, yatay uzunluk her zaman `>= 0` olduğu için
`helix_turns = 0` ve `gamma = atan2(0, H) = 0.0`. Ayrı bir dal yazmana gerek
yok, formül kendiliğinden doğru sonucu veriyor.

Guard clause kullan: `if` bloğunun içine hesap gömme, koşulu erken bitirip
düz devam et.

Docstring: durum ayrımı ve **Durum 2'de yolun optimal olmadığı**, fazlalığın
en fazla `2*pi*rho` olduğu.

- [ ] **Adım 5: Testi çalıştır, geçtiğini gör — CLAUDE**

- [ ] **Adım 6: Commit — CLAUDE**

```bash
git add src/dubins3d.py tests/test_dubins3d.py
git commit -m "Dubins3D: airplane_path, durum ayrimi ve dogrulama"
```

---

### Görev 7: `airplane_length`

**Dosyalar:**
- Değiştir: `src/dubins3d.py`
- Test: `tests/test_dubins3d.py`

**Arayüz:**
- Tüketir: `airplane_path`
- Üretir: `airplane_length(start: Pose3, goal: Pose3, rho: float, gamma_max: float) -> float`

- [ ] **Adım 1: Başarısız testi yaz — CLAUDE**

```python
class TestAirplaneLength:
    def test_matches_path_length(self):
        start = (1.0, 2.0, 10.0, 0.3)
        goal = (40.0, 20.0, 45.0, 1.1)
        p = airplane_path(start, goal, RHO, GAMMA_MAX)
        assert math.isclose(airplane_length(start, goal, RHO, GAMMA_MAX),
                            p.length)

    def test_returns_three_d_not_horizontal(self):
        start = (0.0, 0.0, 0.0, 0.0)
        goal = (30.0, 0.0, 20.0, 0.0)
        p = airplane_path(start, goal, RHO, GAMMA_MAX)
        value = airplane_length(start, goal, RHO, GAMMA_MAX)
        assert value > p.horizontal_length
        assert math.isclose(value, 94.961850, abs_tol=1e-5)

    def test_level_flight_equals_two_d_length(self):
        value = airplane_length((0.0, 0.0, 7.0, 0.0), (40.0, 0.0, 7.0, 0.0),
                                RHO, GAMMA_MAX)
        assert math.isclose(value, path_length((0.0, 0.0, 0.0),
                                               (40.0, 0.0, 0.0), RHO))

    def test_is_asymmetric(self):
        """Dubins asimetrisi 3B'de de gecerli."""
        a = (0.0, 0.0, 0.0, 0.0)
        b = (0.0, 3.0, 5.0, math.pi / 2)
        assert not math.isclose(airplane_length(a, b, 2.0, GAMMA_MAX),
                                airplane_length(b, a, 2.0, GAMMA_MAX))

    @pytest.mark.parametrize("bad", [0.0, -1.0])
    def test_bad_rho_raises(self, bad):
        with pytest.raises(ValueError):
            airplane_length((0.0, 0.0, 0.0, 0.0), (10.0, 0.0, 5.0, 0.0),
                            bad, GAMMA_MAX)
```

- [ ] **Adım 2: Testi çalıştır, başarısız olduğunu gör — CLAUDE**

- [ ] **Adım 3: `airplane_length`'i yaz — OZAN**

```python
def airplane_length(start: Pose3, goal: Pose3, rho: float,
                    gamma_max: float) -> float:
```

Tek satır: `airplane_path`'i aynı argümanlarla çağırıp `.length` özelliğini
döndür. Doğrulama zaten `airplane_path`'in içinde yapılıyor, tekrar yazma.

Docstring: 3B uzunluğu döndürdüğü (yatayı değil) ve planlayıcının mesafe
ölçütü olarak bunu kullanacağı.

- [ ] **Adım 4: Testi çalıştır, geçtiğini gör — CLAUDE**

- [ ] **Adım 5: Commit — CLAUDE**

```bash
git add src/dubins3d.py tests/test_dubins3d.py
git commit -m "Dubins3D: airplane_length"
```

---

### Görev 8: 3B görselleştirme demosu

**Dosyalar:**
- Oluştur: `notebooks/dubins3d_demo.py`

**Arayüz:**
- Tüketir: `airplane_path`, `DubinsPath3D.sample`

- [ ] **Adım 1: Demoyu yaz — CLAUDE**

İki panel:

- **Sol:** helis gerektirmeyen bir yol (uzun yatay mesafe, az irtifa farkı)
- **Sağ:** helis gerektiren bir yol (kısa yatay mesafe, çok irtifa farkı)

`matplotlib`'in 3B ekseni kullanılıyor:

```python
fig = plt.figure(figsize=(16, 8))
ax = fig.add_subplot(1, 2, 1, projection="3d")
```

Her panelde `sample(0.5)` ile alınan noktalar `ax.plot3D` ile çiziliyor;
başlangıç ve hedef işaretleniyor; başlıkta `helix_turns`, `gamma` (derece) ve
3B uzunluk.

Yere izdüşümü de çizilsin (z sabit, en alt seviyede, açık gri) — helisin
yatayda ne yaptığı böyle görünüyor.

`results/dubins3d_demo.png` olarak kaydedilecek, `dpi=150`.

- [ ] **Adım 2: Çalıştır ve şekli incele — CLAUDE**

Çalıştır: `./venv/Scripts/python.exe -m notebooks.dubins3d_demo`
Beklenen: sol panelde düzgün tırmanan tek bir eğri; sağ panelde başlangıçta
sarmal, sonra hedefe uzanan bir yol. İkisinde de z monoton artmalı.

- [ ] **Adım 3: `Çalıştırma Komutları.md`'ye satır ekle — CLAUDE**

- [ ] **Adım 4: Commit — CLAUDE**

```bash
git add notebooks/dubins3d_demo.py results/dubins3d_demo.png "Çalıştırma Komutları.md"
git commit -m "Dubins3D: 3B gorsel demo"
```

---

## Bu planın dışında

- 3B engeller (silindir/küre) ve irtifa sınırları — sonraki tur
- `rrt_star.py`'nin dört elemanlı poza geçmesi — ondan sonraki tur
- Tırmanma ve alçalma için ayrı açı limitleri
- Literatürdeki "orta irtifa" optimal çözümü
- Rüzgâr
